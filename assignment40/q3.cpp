//Returns a reverse iterator which points to the position before the beginning of the
//deque
#include<iostream>
#include<deque>
using namespace std;
int main(){
     deque<int> d={1,2,3,4,5,6,8};
    // deque<int>:: iterator it=d.rbegin();
      //cout<<*(it+1);

    return 0;
}